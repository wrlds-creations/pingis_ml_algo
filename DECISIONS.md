# Decisions

This file is the source of truth for meaningful project decisions. Use `ITERATION_LOG.md` for detailed model metrics, build hashes, data rounds, and device feedback.

## Decision Log

| Date | Decision | Rationale | Decided By | Impact | Revisit Trigger |
|---|---|---|---|---|---|
| 2026-07-01 | Do not promote `p>=0.20` with Fable-noise veto disabled from T0103/T0104C | Love's live test felt much better after lowering `Bounce audio test` to `p=0.20` with noise veto `1.00`, and the corrected T0104 replay confirms why: positives improve from `181/320` at `0.575` to `303/320`. But it already creates `3` false counts in fresh talking-only/racket-handling runs, and the older T0103 safety sweep is clearly unsafe at this setting: `56` boundary negative false counts and `121` Round A hard-negative false counts. Even `p>=0.30/no-veto` remains unsafe on older safety rows (`28` boundary false counts and `70` Round A hard-negative false counts) despite looking cleaner on fresh T0104 | Codex | Keep `p>=0.20/no-veto` as a diagnostic/manual test setting only. Do not change app defaults, export a new model, or promote T0103 based on threshold lowering alone. Next work should ingest T0104B exact labels and train/evaluate a candidate that separates weak real bounces from handling/background/impact negatives | A later candidate or additional safety gate recovers T0104 weak positives while holding fresh T0104 negatives, boundary negatives, rejected T0073 unsafe cases, and Round A hard negatives to an acceptable false-count level |
| 2026-07-01 | Do not promote T0103 as-is after fresh `Bounce audio test` validation | T0104 pulled `16` fresh Motorola debug sessions after Love tested two runs each across six positive scenarios and two negative scenarios. T0104A later resolved the ambiguous slow/high truth: one run is confirmed expected `20`, and one run is excluded as unclear. Corrected included positives counted only `181/320` despite `365` included peak candidates, with `184` low-probability rejections. Far/soft+background was especially weak (`4/60` counted despite `88` candidates). The hard negatives were encouraging (`0` counts from `287` talking-only/racket-handling candidates), but the live recall is too low for kids/home self-practice. The failure is mainly final classifier `below_threshold`, with the unclear slow/high clip excluded from truth | Codex | Keep T0103 behind `Bounce audio test` as diagnostic only. Do not merge, push, or promote it into production/default Fable/studs flows. Next work should use the corrected T0104 validation baseline, exclude the unclear slow/high run, and train/deeper-audit weak live positives while preserving Round A/T0073 hard-negative safety | A later candidate recovers the weak live positives, especially far/soft+background, while maintaining current hard-negative safety in older Round A/T0073 and fresh Motorola validation |
| 2026-07-01 | Export and install T0103 only behind the separate `Bounce audio test` entry | T0103 ingested all nine boundary positive review pages (`269/269` labels, including the `8774` override to `29`) and evaluated a boundary-trained ExtraTrees candidate against fresh boundary positives/negatives plus older Round A/T0073 safety. The selected policy improves the current T0075-style boundary baseline from `209/269` positives and `33` boundary false counts to `225/269` positives and `2` boundary false counts, while keeping Round A `944/960` positives with `4` hard-negative false counts. Far/soft remains weak (`53/90`), so this is worth fresh Motorola validation but not production promotion | Codex | `Bounce audio test` now loads `apps/collector/src/models/fable_extra_trees_candidate_t0103.json` with threshold `0.575`, smart dedupe `180 ms`, and Fable-noise veto disabled. Existing `Fable-algoritm`, `Studsdetektor`, `Studs FH/BH LIVE`, production Fable JSON, camera tracker, and normal app flows are unchanged. The feature branch still must not merge to `main` before device validation and Love approval | Fresh Motorola scenario tests show unacceptable false counts or under-counts, app/debug parity diverges from offline replay, or a later candidate improves far/soft recall without re-opening hard-negative false counts |
| 2026-07-01 | Do not merge the current cleanup branch to `main` before validating the audio model/runtime | Love corrected the cleanup direction: local commits are useful to tidy the mixed feature branch, but merging them to `main` before the current bounce-audio runtime works on device would promote experimental app/model/camera work too early. The branch now contains diagnostic `Bounce audio test`, Fable recorder, audio tooling, and live racket tracker changes that still need scenario-level validation before product promotion | Love + Codex | Local commits on `codex/t0057-fable-auto-improvement-loop` are allowed when explicitly requested, but no merge or push to `main` should happen from this mixed branch. Raw/generated `data/` remains outside git. Future PR planning should either wait for validation or split proven pieces onto a clean branch | Fresh Motorola validation and replay show the selected runtime works by scenario, app entries are classified as product/diagnostic/hidden, and Love explicitly approves PR/merge direction |
| 2026-06-30 | Do not export or install the T0079 live-failure retrain candidate | T0079 trained/evaluated six final-layer candidate families using Round A rows plus the T0076 clean live failure (`10` expected, current app `2`) and noisy/background live failure (`30` expected, current app `6`, `30` manual labels). The old T0075 threshold-only comparison remains the clearest tradeoff: `p>=0.2 smart220` recovers clean `10/10` and noisy `27 TP + 3 FP`, but creates `116` Round A hard-negative false counts; `p>=0.3` gives clean `9/10` and noisy `24 TP / 0 FP`, but still creates `48` hard-negative false counts. The best safe retrained policy (`ExtraTrees leaf2 live p>=0.6 smart220`) keeps hard-negative FP at `0` but still counts only clean `2/10` and noisy `6/30`, essentially no live improvement. The best live-recall policies recover clean/noisy but produce too many talking/handling/floor-table false counts | Codex | Keep `Bounce audio test` guarded. Do not export a new model JSON, build/install an APK, or replace default Fable behavior from T0079. Use the ignored T0079 outputs for diagnosis and plan the next round around more target-domain data and/or a stronger feature/model representation | A new candidate improves clean/noisy Motorola recall while keeping Round A hard-negative false counts acceptably low in out-of-fold/app-style replay, then passes fresh phone validation |
| 2026-06-30 | Do not fix T0076 live under-counting by lowering the T0075 threshold alone | Fresh `Bounce audio test` validation showed the T0075 threshold `0.575` is too conservative on-device: a clean normal-bounce clip was expected `10` but app counted `2`, and a noisy/background clip was expected `30` but app counted `6`. Lowering the classifier threshold helps those clips, but the old reviewed Round A OOF safety replay fails badly: `p>=0.2 smart220` would count the noisy clip as `30` (`27 TP`, `3 FP` among peak-matched labels) and the clean clip as `10/10`, but it also creates `116` Round A hard-negative false counts (`50` talking-only, `33` racket-handling, `33` floor/table/other). `p>=0.3` avoids false positives on the noisy clip but still creates `48` Round A hard-negative false counts. A small spectrogram probe did not clearly outperform T0075; it can be a future feature family, but not a quick replacement | Codex | Keep `Bounce audio test` guarded and do not promote or retune the live app threshold from `0.575` to `0.2`/`0.3` without a new trained/fused candidate and safety replay. The next audio work should use the new live labels as validation/training material and explicitly balance recall against talking/handling/floor/table negatives | A retrained or fused candidate using the new live clips improves clean/noisy Motorola recall while keeping reviewed hard-negative false counts acceptably low in OOF/app-style replay |
| 2026-06-30 | Treat T0075 as export/parity passed, but keep the ExtraTrees Fable candidate inactive until guarded Motorola validation | T0075 refit the T0074-selected `ExtraTrees leaf4 p>=0.575 smart220` classifier from the same T0071 candidate rows, exported the separate app-style JSON `apps/collector/src/models/fable_extra_trees_candidate_t0075.json`, and proved Python-vs-app tree probability parity on Round A plus T0060 C2 rows (`6.66e-16` max diff). Final-fit replay is intentionally optimistic (`959/960` Round A positives, `0` hard-negative false counts) because the model saw the Round A rows during training. The promotion baseline remains T0074's out-of-fold replay: `932/960` positives, `0` hard-negative false counts, rejected manual false positives `0/2`, and T0060 C2 `23/31` | Codex | Keep `fable_audio_model.json`, `audio_model.json`, `audio_contact_model.json`, live counting logic, camera tracker, APK, cloud/API, and AWS unchanged. T0076 may wire this candidate behind a scoped runtime switch and install for fresh Motorola validation, but no production/live replacement is approved from parity alone | App/runtime integration diverges from the parity artifact, fresh Motorola validation misses too many real bounces or false-counts speech/handling/floor/table, or new reviewed data changes the selected threshold/model |
| 2026-06-30 | Use `ExtraTrees leaf4 p>=0.575 smart220` as the next Fable app-export/parity candidate instead of `p>=0.5` | T0074 replayed the existing T0072 `ExtraTrees leaf4` probabilities across `51` threshold/dedupe policies after Love's manual T0073 false-positive review. The original `p>=0.5 smart220` policy counted both rejected floor/table/other false positives (`round_fp_024` and `round_fp_025`) and had `5` hard-negative false counts. A practical threshold of `0.575` rejects both rejected rows and all five manually reviewed false-positive cases, reducing hard-negative false counts `5 -> 0`, while Round A positives change `940/960 -> 932/960`, positive abs error `20 -> 28`, background-sound count `179 -> 171`, speaking/counting remains `200/200`, fast remains `136/136`, and held-out C2 changes `26/31 -> 23/31`. The first mathematically safe threshold was `0.573`, but `0.575` gives the same metrics with margin above the rejected `0.572287` row | Codex | Keep current app runtime, model JSON, thresholds, APK, camera tracker, cloud/API, and AWS unchanged. T0075 should first create/export an app-style runtime artifact and parity replay for `ExtraTrees leaf4 p>=0.575 smart220`; only then should a Motorola APK/live validation round be considered | App-style feature/parity replay diverges from Python, fresh Motorola validation loses too many true racket bounces, or new reviewed hard negatives show unacceptable false counts at `p>=0.575` |
| 2026-06-30 | Require T0074 to handle the rejected T0073 floor/table/other false positives before any APK install | Love manually reviewed the five T0073 selected-policy hard-negative false counts. `round_fp_021`-`round_fp_023` are acceptable/ambiguous because they sound ball-bounce-like, but `round_fp_024` and `round_fp_025` should not count. The selected `ExtraTrees leaf4 p>=0.5 smart220` policy still counts those two floor/table/other impacts at near-threshold probabilities (`0.559` and `0.572`), so exporting the policy as-is would knowingly carry unsafe hard-negative behavior | Love + Codex | Keep app runtime, model JSON, thresholds, APK, camera tracker, cloud/API, and AWS unchanged. Update T0073 artifacts with manual review labels. T0074 should be app-style parity plus threshold/safety-gate tuning, with `round_fp_024` and `round_fp_025` treated as must-watch/must-not-count rows before any APK install or live validation | A threshold/safety gate that rejects `round_fp_024`/`round_fp_025` causes unacceptable recall loss on reviewed positives or fresh Motorola validation shows the live tradeoff is worse than the offline pack suggests |
| 2026-06-30 | Treat the T0072 peak-candidate classifier/veto as an offline candidate worth bad-case review before export, but do not change app runtime yet | T0072 used T0071 scenario-diverse reviewed labels (`960` positives across `22` Round A clips plus `741` hard-negative peak candidates) and leave-one-session-out replay over `1778` `Peak fast balanced` candidates. Best screened policy `ExtraTrees leaf4 p>=0.5 smart220` strongly beat current Fable and peak-only by bucket: Round A positives `940/960`, positive abs error `20` vs current `182`, expected-zero false counts `5` vs current `164`, talking-only `0`, handling `3`, floor/table/other `2`, speaking/counting `200/200`, fast `136/136`, and background `179/196`. It also scored held-out T0060 C2 exact `26/31` with `0` FP. Remaining risk: background still misses `17`, C2 misses `5`, and this is offline Python replay rather than app-export/runtime parity or fresh live validation | Codex | Keep app runtime, Fable model JSON, thresholds, camera tracker, APK, cloud/API, and AWS unchanged. T0072 artifacts are diagnostic/local. Next work should inspect selected-policy misses/false positives, prepare an app-style export/parity path if the bad cases are acceptable, then build/install only for a fresh Motorola validation round. Do not claim production readiness from T0072 alone | Bad-case review shows the misses/false positives are unacceptable, app-style parity diverges from Python replay, or fresh Motorola validation fails; conversely, parity plus fresh live validation passes with acceptable kids/home-practice behavior |
| 2026-06-29 | Do not export the first trained peak-candidate classifier/veto | T0070 trained four local diagnostic classifiers on `Peak fast balanced` candidates using T0066's exact reviewed background-sound positives (`196`) plus candidate hard negatives (`550`) and replayed session-held OOF metrics, T0060 held-out C2, and full Round A. The best OOF classifier looked strong on the narrow exact-label slice (`177/196` TP, `19` missed, `5` positive-clip FP, `21` selected expected-zero false counts), and RF variants could hit `31/31` on the T0060 C2 holdout. But full Round A failed by scenario: conservative classifier settings mostly learned the background-sound slice and missed normal/slow/fast/messy/speaking positives, while lower thresholds recovered more positives but false-counted talking heavily. No candidate beat current runtime and T0069 chains by bucket | Codex | Keep app runtime, Fable model JSON, peak/RMS gate, camera tracker, export, APK, cloud/API, and AWS unchanged. Treat T0070 artifacts as diagnostics only. The immediate bottleneck is exact positive-label diversity plus hard-negative safety, not estimator type or a quick threshold. The next work should label or derive candidate rows for normal/slow/fast/messy/speaking positives and enough talking/handling/floor-table negatives, then rerun bucket-level app-style replay | Exact labels or a validated pseudo-labeling method cover all Round A positive scenarios and a new classifier/veto beats current RMS/Fable and T0069 baselines by bucket, then passes fresh Motorola validation |
| 2026-06-29 | Do not replace the current Fable runtime with the tested peak + current-Fable/veto/dedupe chains | T0069 tested `19` offline pipelines using `Peak fast balanced` candidates followed by current Fable app logic, simple racket-probability gates, surface/noise vetoes, and smart dedupe. Current RMS/native + Fable baseline scored T0066 exact `41/196` TP with `156` selected expected-zero false counts and Round A `810/965` positives with `164` negative false counts. The best exact-F1 peak chain (`p_racket>=0.02 + smart300`) recovered T0066 background contacts (`160/196`) but was unsafe on Round A negatives (`285`, including `104` talking-only). The best conservative aggregate chain (`p_racket>=0.5 + smart240`) improved aggregate positives and total negatives (`840/965`, negatives `126`) but regressed talking-only false counts (`22` vs baseline `8`) and still counted only `93/200` background positives | Codex | Keep app runtime, Fable model JSON, APK, camera tracker, export, cloud/API, and AWS unchanged. `Peak fast balanced` remains the best raw candidate source, but the next step should be a trained classifier/veto evaluated in the same app-style replay before any runtime replacement. RMS/native remains the baseline/fallback | A trained classifier/veto on peak candidates improves T0066/T0060 recall, keeps Round A talking/handling/floor-table false counts at or below baseline by bucket, and passes fresh Motorola validation |
| 2026-06-29 | Treat `Peak fast balanced` as the next primary candidate-gate candidate, but keep RMS as fallback until hybrid replay passes | T0068 directly compared current RMS/native candidate generation with multiple RMS and peak configs. Current RMS/native had exact background recall `0.714` at `140 ms`, `1025` selected expected-zero candidates, median timing delta `31.0 ms`, and Round A fast candidates `151/135`. `Peak fast balanced` (`raw_abs`, `3 ms` smoothing, `220 ms` gap, `abs_min=0.08`, `ratio_min=2`) had exact recall `1.000`, only `358` selected expected-zero candidates, median timing delta `1.5 ms`, and Round A fast candidates `136/135`; it also reduced Round A negative candidates from current RMS `1583` to `741`. T0067 strict peak was safer but too slow for fast bounces (`97/135`) | Codex | Candidate-gate evidence supports using `Peak fast balanced` as the primary gate candidate in the next hybrid peak+classifier/veto replay. Do not remove RMS/native from runtime yet; keep it as baseline/fallback until the full peak-candidate + classifier/veto + smart-dedupe chain beats current app behavior in app-style replay and live Motorola testing. No app runtime, model JSON, APK, export, cloud/API, or AWS change happened in T0068 | Hybrid replay shows peak candidates overload the classifier, create duplicate counts, or regress final false counts versus current RMS+Fable behavior |
| 2026-06-29 | Use waveform peaks as a candidate generator, not as a standalone live counter | T0067 tested a live-style peak gate on T0066 reviewed background-sound positives and selected expected-zero negatives. The best simple gate found `192/196` exact reviewed background contacts versus current Fable `41/196`, and reduced selected expected-zero false counts from `156` to `46`. But full Round A block replay with the same gate counted `130` talking-only false contacts, `24` floor/table/other impacts, `22` racket-handling impacts, and missed fast racket contacts (`97/135`). | Codex | Keep app runtime, model JSON, thresholds, APK, camera tracker, training export, cloud/API, and AWS unchanged. The peak picker is approved for review prefill and for the next hybrid candidate-generator experiment, but it must be paired with speech/noise/impact veto or classifier logic and dynamic cooldown before runtime replacement. | A hybrid peak-gated candidate passes exact reviewed positives, expected-zero hard negatives, Round A count replay, and fresh live Motorola validation without speech/handling/table/floor regressions |
| 2026-06-29 | Use Round A background-sound labels as reviewed positives, but keep expected-zero triggers as candidate hard negatives until confirmed in the training ticket | T0066 converted the four `Racket bounce + background sound` clips into exact reviewed positive timestamps: `196` racket labels versus `200` recorder expected contacts, while current app/replay counted only `55`. These are direct reviewed positive labels. The selected expected-zero `Racket handling, no bounce` and `Floor/table/other impact, no racket` clips produced `1025` current-Fable trigger candidates; the recorder scenario says they should contain no racket contacts, but they have not been individually timestamp-reviewed, so they should enter the next step as candidate hard negatives with scenario-level confirmation rather than silently becoming permanent ground truth | Codex | T0067 used the `196` reviewed positives as high-value hard-positive material and reported expected-zero negatives separately. Future T0068-style training/replay may use the `1025` expected-zero trigger candidates for safety replay and candidate training only with explicit scenario-role tagging. No model JSON, app runtime, APK, or thresholds changed in T0066/T0067 | A manual review finds real racket contacts inside an expected-zero clip, or a candidate trained with these negatives suppresses real racket recall in held-out replay |
| 2026-06-29 | Treat Fable recorder Round A as block-level evidence until exact timestamps are reviewed | T0065 pulled `34` Round A recorder clips plus `1` smoke test and verified strong metadata/WAV integrity. The clip-level counts are valuable for scenario diagnosis, and current bundled Fable replay already shows the main failures: positives `810/965`, negatives false-counted `164/0`, background-sound positives only `55/200`, racket-handling false-counts `108`, and floor/table/other false-counts `48`. However, positive clips store expected total contacts rather than exact contact timestamps, so using them directly as event-level training rows would be noisy | Codex | Keep model JSON, thresholds, app runtime, APK, training/export scripts, cloud/API, and AWS unchanged. Use Round A first for exact timestamp review/training-row planning and scenario-level replay; do not export or train a promoted candidate from clip totals alone | Exact timestamp labels or a validated row-generation method convert selected Round A clips into reviewed positive/negative training rows and app-style replay improves without hard-negative regression |
| 2026-06-29 | Add a separate Fable data recorder before retraining/export | T0063 showed the current bundled Fable path is weak on the held-out C2-like domain, but the immediate bottleneck is balanced target-domain audio, not another threshold tweak. A separate recorder lets Love collect full continuous WAVs with scenario/count metadata, including hard positives and negatives, without disturbing the current live counting or camera flows | Love + Codex | T0064 adds a DATA-mode `Fable data recorder` that saves `.wav + .json` pairs under `Download/pingis_sessions/fable_training_audio/` only when Love chooses save. Fable live counting, model JSON, thresholds, camera tracker, training/export scripts, cloud/API, and AWS resources remain unchanged. Next step is install/smoke/collect/pull before any model rebuild | The recorder is too slow or awkward for collection, or collected WAV+metadata plus review can be replaced by a better in-app exact timestamp labeling workflow |
| 2026-06-29 | Do not export or build after T0063 exact T0060 replay | T0063 ingested Love's saved `31` manual racket labels for `fable_live_session_2026-06-29T13-29-50-713Z` and reran a current-data-only replay using only the T0060 WAV, the exact labels, the current bundled `fable_audio_model.json`, and optional metadata from the same T0060 debug JSON. Saved app JSON counted `1`; full-WAV current Fable replay counted `7` with exact `5 TP / 2 FP / 26 missed` at `140 ms` tolerance. Centered on Love's labels, the current model predicted only `17/31` as `racket_bounce`, and only `5/31` cleared the current threshold. Trigger timing also contributes because only `19/31` true labels are within the current `140 ms` trigger window | Codex | Keep app runtime, model JSON, thresholds, APK, camera tracker, Roboflow/cloud/API, and training export unchanged. Treat T0060 labels as held-out evidence and possible future hard-row material only after an explicit training-role decision. Next audio work should rebuild/restore broader 4-class train/val material and run app-style safety replay before any promotion | A broader 4-class candidate using approved hard positives/negatives improves held-out C2-like recall and keeps table/floor/speech/handling false counts acceptable in app-style replay |
| 2026-06-29 | Do not export the current diagnostic Fable candidate after T0060 held-out failure | T0060 pulled Love's fresh C2-like `Fable-algoritm` run (`fable_live_session_2026-06-29T13-29-50-713Z`). Love reported `31` real racket contacts and app count `1`; the pulled JSON confirms `59` native candidates and `1` counted event. The best local diagnostic candidate (`rf_fast_balanced`, `t0049_plus_c2`, threshold `0.7`) also counted only `1/31` on the fresh held-out WAV, with `58` low-probability rejections. This means the candidate learned the earlier C2 slice poorly and does not generalize to the new speech/background C2-like run | Codex | Keep app runtime, model JSON, thresholds, APK, and camera tracker unchanged. Next audio work should label exact timestamps in the T0060 held-out WAV and rebuild a broader 4-class replay with hard speech/background positives plus table/floor/noise safety negatives before any export/build/install | A new candidate trained/evaluated with exact T0060 labels and broader 4-class safety counts the held-out-like domain reliably without unacceptable table/floor/speech/handling false counts |
| 2026-06-29 | Do not treat the current local reviewed audio pull as a 4-class Fable safety set | T0059 fixed recursive local reviewed discovery and found `44` local reviewed records under the existing pulled audio sessions, all `racket_bounce`. The diagnostic candidate predicted `43/44` as racket, which is useful ordinary quiet-racket positive coverage, but there are `0` local reviewed table/floor/noise safety negatives in this workspace | Codex | Keep app runtime, model JSON, thresholds, APK, and camera tracker unchanged. Future promotion still needs fresh held-out C2-like phone data and broader table/floor/noise safety, either from restored historical reviewed material or new labeled collection | Local reviewed pulls include enough confirmed table/floor/noise/speech/handling negatives to evaluate safety by bucket |
| 2026-06-29 | Treat held-out replay plus table/floor safety as mandatory before any Fable export | T0058 extended the T0057 local loop with optional held-out continuous-WAV replay and capped TT Sounds safety diagnostics. The same best diagnostic candidate still improves C2 compared with current Fable, but a tiny TT Sounds sample (`5` clips per label) predicted racket for `11/15` non-racket clips, including `5/5` floor and `5/5` table bounces. This confirms that optimizing C2/speech recall alone can create an unsafe live counter | Codex | Keep app runtime, model JSON, thresholds, APK, and camera tracker unchanged. Future audio candidates must pass a fresh held-out C2-like phone replay and table/floor/noise safety checks before export/build/install | A broader 4-class candidate or well-tagged local dataset improves held-out C2-like recall while keeping table/floor/speech/handling false counts acceptably low |
| 2026-06-29 | Run Fable audio improvement as an autonomous local loop but stop before export until held-out proof exists | Love asked Codex to keep improving audio autonomously and only request input when needed. T0057 added a repeatable local loop that trains diagnostic candidates from T0049/T0055, replays exact C2 truth, and checks T0050 targeted blocks as weaker block-level safety evidence. The best fast local candidate (`rf_fast_balanced`, `t0049_plus_c2`, threshold `0.7`) reaches exact C2 `18` TP / `0` FP / `12` missed and reduces T0050 negative false counts from `5` to `1`, but it uses C2 rows in training, remains binary `racket_bounce` vs `noise`, and has no table/floor safety proof | Love + Codex | Keep app runtime, Fable model JSON, thresholds, APK, and camera tracker unchanged. T0057 artifacts stay ignored under `/data/`. Codex can rerun the loop when new data arrives, but the next user input needed is a fresh held-out C2-like continuous-WAV test or restoring/building the missing full historical 4-class train/val data | A fresh held-out C2-like run plus broader table/floor/speech/handling replay shows a candidate improves recall without unacceptable false counts |
| 2026-06-29 | Do not export the T0056 binary Fable candidate despite improved C2 replay | T0056 showed the timing/window problem is not enough to explain C2: exact saved app clips for the `29` phone-trigger rows Love labeled as racket were all saved as `noise`, corrected timestamps gave only `2/30` current-model racket labels, and raw/bandpass peak recentering also failed. T0049-only local candidates still failed C2 (`1` TP / `3` FP / `29` missed). Adding the T0055 C2 hard rows locally improved C2 replay to `22` TP / `3` FP / `8` missed, but that candidate is binary `racket_bounce` vs `noise`, uses the target C2 rows in training, and has no table/floor safety proof | Love + Codex | Keep app runtime, Fable model JSON, thresholds, APK, and camera tracker unchanged. T0056 artifacts stay ignored under `/data/`. Next work should be broader 4-class Fable candidate retrain/replay using full table/floor/noise safety material plus these hard rows, or a fresh held-out C2-like validation round before any export/build/install | A broader 4-class candidate beats current Fable on C2-style, normal/high/fast/messy, talking/handling, table, and floor replay without unacceptable false counts |
| 2026-06-28 | Use the T0055 reviewed C2 labels as a hard slice, not a standalone retrain set | Love labeled the T0052 continuous C2-style run in the T0053/T0054 UI. T0055 ingested `49` reviewed rows: `30` racket contacts and `19` noise labels. `29` racket labels were attached to saved phone-trigger rows, but many required timing adjustment, so this is not exact native-hit coverage. The current app counted `0/30`, and re-centering the current Fable model on Love's reviewed timestamps only predicted racket for `2/30`, so the failure is mostly model/domain coverage, not just timestamp drift. The clip is only `20.5 s`, with one setup and background, so it is too small to safely retrain/export by itself | Love + Codex | Keep app runtime, Fable model JSON, thresholds, APK, and camera tracker unchanged. T0056 should start with a feature/window audit, then combine this hard C2 slice with approved local positives and hard negatives for app-style candidate retrain/replay. Generated T0055 artifacts stay ignored under `/data/` | A broader approved local dataset shows the same failure mode and supports a candidate that improves messy/speech recall without increasing talking/handling/table/floor false counts |
| 2026-06-28 | Treat the continuous C2-style Fable failure as a model/domain rejection problem, not a threshold or camera problem | T0052 pulled the first continuous-WAV Fable hard-mode run after T0051. Love reported `30` real racket contacts and app count `0`. The saved JSON had `48` native candidates, but `44` were classified as `noise` and rejected as `not_racket`, while `4` early rows were `stale_backlog`. The full-WAV offline gate found the same trigger-count scale, so the mic/native gate was active; the bundled Fable model did not recognize these contacts under speaking/counting plus kid/background sound | Love + Codex | Keep app runtime, Fable model JSON, thresholds, camera tracker, and APK unchanged. Do not raise confidence thresholds or add a speech veto for this failure because that would worsen recall. Next work should plan a full local Fable candidate retrain/replay or feature/window audit using exact timestamp labels from hard positives plus talking/handling hard negatives | A reviewed timestamped hard-mode dataset shows native onset misses dominate instead, or a retrained/replayed candidate fails to improve C2-style recall without increasing false positives |
| 2026-06-28 | Add continuous WAV capture to Fable live debug before the next messy-audio retrain decision | T0050 C2 was valid hard-mode practice, but the saved artifact was only discontinuous event-centered `audio_b64` snippets. Love confirmed the stitched snippets missed real bounce sounds, so the current debug dump cannot separate native onset misses from model rejections. Writing a continuous WAV from the same native mic stream gives a complete timeline without starting a second recorder | Love + Codex | `Fable-algoritm` now saves a continuous 22,050 Hz mono WAV next to each debug JSON and records its metadata in `continuous_audio`. The Fable model, thresholds, counting logic, camera tracker, and model artifacts remain unchanged. Next validation should collect a fresh C2-style run and compare the continuous WAV truth against saved event rows | A fresh continuous-WAV Fable run shows the remaining misses are already explainable from event snippets, or a separate reviewed collection flow replaces Fable debug as the promotion gate |
| 2026-06-28 | Do not ship a Fable gate from the T0050 targeted debug round | T0050's focused Fable-algoritm round showed broader live reliability problems than speech false positives. Mapped real-racket blocks expected `196` counts but saved/app counted `132`, with severe misses on fast racket (`23/40`) and messy failed practice (`0/40`). Negative blocks still false-counted `5` times, mostly talking/racket handling. The conservative T0048/T0049 partial veto would remove only `1/5` mapped negative false counts with saved audio while also rejecting `2` counted real bounces, so it is not a product fix | Love + Codex | Keep `nr_bounce_v5_2026_06_12`, app runtime, thresholds, model JSON, camera tracker, and APK unchanged. Treat T0050 as holdout/replay evidence until Love explicitly approves using the round as training data. Next work should build a full candidate retrain/replay plan that addresses recall and hard negatives together | Love confirms T0050 blocks are trainable and a retrained/replayed candidate beats current Fable on normal/high/fast/messy/talking/handling blocks without regression |
| 2026-06-28 | Do not promote the T0049 tiny trainable Fable speech veto | T0049 reused Love-approved June 28 Fable debug clips as local candidate material. Apparent fits could memorize the four speech false positives, but held-out checks were not strong enough: counted-row trainable folds caught at most `53.6%` held speech false positives while still rejecting real racket scenarios. Context models trained with already-rejected speech rows caught more speech (`27/28` for a depth-2 tree), but rejected too many real racket events (`11/141`). The best low-loss context threshold caught `21/28` repeated speech cases while rejecting `1/141` real racket event. The only zero-real-loss useful gate remains the T0048 partial ratio gate at `2/4` speech false positives and `0/141` real racket loss in this tiny set | Love + Codex | Keep `nr_bounce_v5_2026_06_12`, app runtime, model JSON, camera tracker, and APK unchanged. Preserve the ignored T0049 approved extra-row package (`148` racket positives, `268` speech/noise negatives) for a future full retrain once the full historical processed training split is available | A larger local holdout validates the conservative partial ratio veto, or a full retrain using more speech-that-fools-the-model plus messy positives beats the current model in app-style replay without normal/messy recall regression |
| 2026-06-28 | Do not ship a simple Fable speech veto from T0048 | T0048 decoded `205` app-domain snippets from the fresh Motorola Fable debug round. The best all-speech simple gate, `clip_abs_peak >= 0.22299`, rejects all `4/4` talking false positives but also rejects `5` real racket counts. The best conservative partial gate, `nr_bp_peak_ratio > 0.76144`, rejects `2/4` talking false positives and `0/141` counted real racket events in this tiny set, so it is not broad enough to trust. No reviewed local speech/noise training sessions are currently present in pulled raw data | Love + Codex | Keep `nr_bounce_v5_2026_06_12`, app runtime, model JSON, camera tracker, and APK unchanged. Next ticket should collect or explicitly approve targeted local speech hard negatives plus messy low-confidence positives, then use app-style replay before any model replacement or runtime gate | A larger local holdout proves a partial speech veto removes false counts without recall loss, or a trained local candidate beats the current model in app-style replay |
| 2026-06-28 | Audit speech false positives and messy-practice recall before retraining broadly or returning to camera-side work | T0047 fresh Motorola validation showed the current Fable trigger is strong for isolated self-practice blocks (`20/20` normal, `20/20` high, `30/30` fast), but realistic messy practice was variable (`13/20`, then `18/20` and `20/20`) and a focused talking retest produced `4` false counts. The four talking false positives were all counted as `racket_bounce`; a confidence-only threshold high enough to remove them would drop real-bounce counts badly (`0.95` would leave normal `11/20`, messy A `4/20`, messy B `8/20`) | Love + Codex | Keep `nr_bounce_v5_2026_06_12` and app behavior unchanged for now. Next work should decode/listen to speech false-positive snippets and low-confidence real racket snippets, then test a speech/continuous-noise veto, gate adjustment, or targeted hard-negative retrain before changing `Studs FH/BH LIVE` camera-side logic | Speech false-positive audit shows a simple acoustic veto/gate can remove false counts without recall loss, or targeted local hard-negative training beats the current model in app-style replay |
| 2026-06-28 | Do not tune or retrain Fable from the available T0046 phone data alone | T0046 found useful Motorola artifacts but not a balanced promotion set: the reviewed replayable WAVs were old quiet-racket sessions with `44` racket markers and no table/floor/talking negatives, while recent `Studs FH/BH LIVE` side-debug files had tracker/count metadata but no `audio_b64` clips. The old quiet-racket replay was decent (`38` TP, `2` FP, `6` missed), but it does not cover the failure modes seen in TT Sounds or Love's recent live bounce tests | Love + Codex | Keep `nr_bounce_v5_2026_06_12` unchanged. Next work should collect a fresh local Fable/audio-debug validation round with known expected counts across racket bounces, table/floor sounds, and normal home distractions before any threshold/model change | A fresh Motorola validation round provides enough positives and hard negatives to make a measured threshold/training decision |
| 2026-06-28 | Prioritize Motorola/local audio evidence before more camera-side live work | T0045 ran the bundled `nr_bounce_v5_2026_06_12` Fable model on real TT Sounds: clip-level racket precision/recall was `0.818792`/`0.215548`, synthetic-live precision/recall was `0.828244`/`0.127797`, and most live-style false positives came from table-bounce clips. TT Sounds clips are short external diagnostics and CC BY-NC 4.0, so they are not enough to promote or reject a shipped model by themselves | Love + Codex | Next work should collect/pull local Motorola Fable audio/debug sessions and use them as the promotion gate before training, threshold changes, or more side-tracker logic. Keep TT Sounds under ignored `/data/` for diagnostics only | Motorola/local reviewed live data shows the installed Fable trigger is reliable enough, or a new local candidate demonstrably improves recall without table/floor/talking false counts |
| 2026-06-28 | Audit Fable audio reliability before changing camera-side logic again | `Studs FH/BH LIVE` still depends on audio as the bounce/count trigger, so camera-side improvements cannot feel reliable if the audio detector misses or false-counts bounces. TT Sounds provides useful external racket/table/floor/other coverage, but its CC BY-NC 4.0 license makes it evaluation/internal-diagnostics material unless permission is confirmed | Love + Codex | T0044 adds an evaluation-only script for the bundled `fable_audio_model.json`, with clip-level classification, synthetic live replay, local reviewed-session replay when data exists, and Fable debug parity checks. It does not train, export, build, install, change app runtime, or replace model JSON artifacts. Local Motorola reviewed/live data remains the promotion gate | Audit shows Fable audio is already reliable enough, local Motorola failures require targeted local collection, TT Sounds permission is obtained for training, or Love prioritizes a camera-only detector despite audio risk |
| 2026-06-26 | Gate visible tracker boxes and side decisions at 95% confidence for live testing | Love observed that when the racket is absent, the prototype can still label other objects as `racket-red` or `racket-black`. For kid-friendly live UX, a missing box is less harmful than a confident-looking wrong box or wrong FH/BH count | Love + Codex | `Studs FH/BH LIVE` keeps native tracking running, but the green box and tracker-based FH/BH side decision only use tracks with confidence >= `0.95`. Lower-confidence tracks are hidden in the UI and counted as `OSAKER` when audio detects a bounce. This is a UI/runtime gate, not a trained detector | Real racket views are hidden too often, `OSAKER` becomes too high, or a calibrated trained detector replaces heuristic confidence |
| 2026-06-26 | Harden black-side live tracking before training a detector | Device feedback showed the no-training tracker followed red reasonably but confused black hair and black shirts with black rubber. A live screenshot also showed the first hardened build rejected real close-up black rubber as too large, so black needs both stricter body/clothing rejection and a close-up compact-rubber allowance | Love + Codex | T0043 keeps Fable audio, model JSON artifacts, and the live `Studs FH/BH` flow unchanged. Broad/tall/body-like dark blobs are rejected or downgraded, large compact oval-ish black rubber can become `racket-black`, and weak black evidence counts as `OSAKER` instead of a wrong FH/BH side | Hardened tracker still follows hair/clothes as `racket-black`, black rubber is now missed too often, or Love wants the live box to follow the racket reliably enough to justify training an on-device detector |
| 2026-06-25 | Use a no-training continuous color/shape racket tracker for the first live racket box | The public Roboflow table-tennis model was too weak for reliable racket following in the kid-practice setup, while T0040 showed the red/black racket-side signal is useful when ROI localization is good. A local tracker keeps privacy/offline behavior and gives Love immediate visual feedback before investing in training a detector | Love + Codex | T0042 adds a CameraX-side red/dark blob tracker, live green racket box, `racket-red`/`racket-black`/`racket` labels, timestamped track lookup for Fable audio bounces, and debug metadata. Fable audio and all model JSON artifacts remain unchanged | Device testing shows the tracker loses the racket too often, black rubber is confused with dark backgrounds/clothes, box alignment is mirrored/wrong, or a trained on-device detector becomes necessary |
| 2026-06-17 | Guard live bounce-side prediction with visible racket color before retraining | The latest `Studs FH/BH LIVE` debug dumps showed Fable audio correctly producing `racket_bounce` anchors while raw `bounce_side_v2_2026_06_11_underangle` side predictions collapsed toward `backhand` on visibly red crops. TypeScript parity passed, so the immediate failure was not a broken runtime export but side resolution/domain bias | Codex | T0041 keeps Fable and `bounce_side_model.json` unchanged, adds a runtime red/black visible-color resolver that respects the selected forehand color, and records raw side/confidence plus color evidence in live debug dumps. A new APK was installed on Motorola for validation | Fresh T0041 live debug dumps still show clear red-forehand crops counted as backhand, color evidence is often absent/occluded, or a retrained side model beats the guarded runtime safely |
| 2026-06-09 | Start video bounce-side work as audio-anchored snapshot data collection, not a long motion classifier | Love wants the model to hear each bounce and inspect wrist/racket side at the impact moment. Current ML Kit pose can see body/wrist landmarks but is not reliable enough for palm/racket roll by itself; MediaPipe Hand Landmarker is a likely later candidate, but it should not be added before we have real Motorola examples and know whether pose-only data is enough | Love + Codex | T0037 adds a separate `Video studs FH/BH` DATA path that imports MP4, uses embedded audio peaks as pending bounce anchors, labels FH-side/BH-side/unknown, and saves `video_bounce_side_snapshot` JSON under a separate device folder. T0038 should pull/analyze the first saved sessions before any model/dependency work; T0039-T0041 remain model/prototype follow-ups | First collected videos show audio peaks are poor anchors, wrist/racket side is not visible at impact, or pose-only features are clearly insufficient and Hand Landmarker/native work is justified |
| 2026-06-04 | Improve perceived playing-retro review startup with profiling, pose deferral, and exact-output runtime cleanup before deeper rewrites | T0033 proved the app output path is now locally reproducible, so performance work can proceed without changing labels or model behavior. Whole-video pose can compete with the audio review-start path, and `spel_retro_audio` had one safe repeated-work cleanup: candidate timestamps were sorted once globally and then sorted again per candidate | Codex | T0034 adds `[review-start-profile]` timing logs and a small `Timing:` diagnostic row. T0035 defers new whole-video pose scans until primary audio markers are ready while preserving saved pose candidates. T0036 reuses the same unique sorted candidate timestamp list inside retro prediction, preserving exact output. These are local app changes only until a later APK build/install | Device timing logs still show unacceptable wait after T0036, or output differs from T0033 app-feature replay |
| 2026-06-04 | Close T0033 as parity/replay-only and resume review-start profiling in T0034 | The new Node app-feature dataset generator and app-gated replay reproduced the T0032 Motorola output exactly on `audio_session_2026-06-04_006`: raw candidate predictions `85 non_target / 75 table_bounce / 59 racket_contact`, editable marker replay `130 TP / 0 wrong / 0 FP / 9 missed`. This proves the earlier `133/0/0/6` expectation was a Python/librosa replay mismatch, not an app candidate-generation failure | Codex | No T0033 retrain/export/APK is promoted. Future `spel_retro_audio` promotion must be judged with app-style output replay. T0034 should now measure first-run review-start latency before T0035 pose deferral or T0036 JS optimization | App-feature replay cannot be scaled to full training/promoted model evaluation, or a future app build changes feature/runtime output |
| 2026-06-04 | Resolve `spel_retro_audio` app-feature/output parity before the next model promotion | T0032 same-clip audit showed the T0031 APK did not reproduce the T0030 Python replay exactly. The app and Python saw the same 219 candidate IDs, but app confidences were systematically lower and three extra racket truths were missed: two from label/probability differences and one from the recovery racket confidence gate. This means the old parity check was too weak because it validated JSON/tree metadata, not the full app feature/output path | Codex | T0033 must build a deterministic app-style replay/training path and keep `audio_session_2026-06-04_008` diagnostic-only. No new `spel_retro_audio` model/APK should be trusted solely on Python/librosa replay until app-style output parity is measured. `studs_live`, ordinary bounce, `audio_model.json`, `audio_contact_model.json`, and video models remain out of scope | T0033 proves the mismatch is fully explained and either fixes the app/runtime feature path or trains/tunes a candidate against the app-style feature surface |
| 2026-06-04 | Install the T0031 APK with the T0030 `spel_retro_audio` model on Motorola | Love approved installing the T0030 candidate so the same reviewed clip can be imported again and checked against the replay expectation. Export parity passed, TypeScript/root validation passed, the release APK contains T0030 and not T0026, and `adb install -r` succeeded on `ZY22L6NDHV` | Love + Codex | Current Motorola baseline is now `playing_retro_audio_rf_v2026_06_04_t0030_multi_window_context` with review thresholds racket `0.0`, table `0.0`, and 80 ms same-label dedupe. APK SHA256 is `7ADADA4889A23B7CC67B3A57E7F9537CB824F554C57510B16BD5DA3EBA0BD625`, installed at `2026-06-04 16:02:09`. `studs_live`, ordinary bounce, `audio_model.json`, `audio_contact_model.json`, and video models remain out of scope | Same-clip device review does not approach the replay expectation, table threshold `0.0` creates too much noise, or app parity/build assumptions are contradicted by saved session JSON |
| 2026-06-04 | Proceed to T0031 export/build/install with the T0030 `spel_retro_audio` candidate if app parity passes | T0030 trained `playing_retro_audio_rf_v2026_06_04_t0030_multi_window_context` from 4,823 rows across 19 playing sessions after adding `audio_session_2026-06-04_006`. Marker replay versus the installed T0028 baseline across six promotion sessions improved TP/wrong/FP/missed from `964/5/13/51` to `984/1/9/35`; on the new session it improved `120/4/4/15` to `133/0/0/6` | Codex | T0031 should export only the separate `playing_retro_audio_model.json`, apply selected review thresholds racket `0.0` and table `0.0`, validate app/Python parity, build a release APK, and install on Motorola. `studs_live`, ordinary bounce, `audio_model.json`, `audio_contact_model.json`, and video models remain out of scope | App export parity fails, TypeScript/build fails, or device review shows table threshold `0.0` creates too much review noise |
| 2026-06-04 | Pause T0029 performance profiling and resume the one-reviewed-video model loop with `audio_session_2026-06-04_006` | Love reviewed a new T0028 `Ljud + video ML` audio session and still had to add markers, so the highest-value next step is to use that fresh correction signal before spending time on review-start latency instrumentation | Love + Codex | T0030 pulls/audits/trains/replays a local `spel_retro_audio` candidate from the new reviewed session plus historical playing data. T0029/T0030-T0032 performance tickets are deferred and renumbered later if needed. No ordinary `Studsdetektor`, app JSON export, APK build, or video model change happens in T0030 | Replay shows the new data does not improve safely, or Love prioritizes latency again before another reviewed-data loop |
| 2026-06-04 | Profile first-run `Ljud + video ML` playing review start before more optimization or retraining | Love reported that even a one-minute clip takes too long before the waveform/review markers appear, and the same video is rarely reopened, so cache would not solve the common customer path. The quality requirement is unchanged: same model, same candidates, same thresholds, and same marker output until a measured bottleneck justifies a scoped optimization | Love + Codex | T0029 should measure import, audio extraction, WAV decode, waveform/review-data generation, peak candidates, recovery, `spel_retro_audio` feature/RF work, marker creation, and pose start/end when resumed. Its original follow-up sequence is deferred because T0030 is now the new reviewed-data loop | T0029 shows the bottleneck is already acceptable, Love prioritizes a newly reviewed data audit over performance, or a later product flow makes repeated reopen/cache important |
| 2026-06-04 | Proceed to T0028 export/build/install with the T0026 `spel_retro_audio` candidate | T0027 replayed `playing_retro_audio_rf_v2026_06_04_t0026_multi_window_context` against the installed T0024 marker baseline across 05-28_002, 05-29_001, 05-29_002, 06-03_005, and 06-04_001. Selected settings are racket threshold `0.0`, table threshold `0.45`, and 80 ms same-label dedupe. Marker replay improved TP/wrong/FP/missed from `706/22/26/153` to `844/1/9/36`, and 06-04 improved from `130/0/2/35` to `154/0/0/11` | Codex | T0028 should export only the separate `playing_retro_audio_model.json`, apply the selected review thresholds, validate app/Python parity, build a release APK, and install on Motorola if validation passes. `studs_live`, ordinary bounce, `audio_model.json`, `audio_contact_model.json`, video models, and Review UX stay out of scope | App export parity fails, TypeScript/build fails, or Motorola review shows the lower table threshold creates too much review noise |
| 2026-06-04 | Proceed to T0027 replay/tune with the T0026 `spel_retro_audio` candidate before app export | T0026 trained `playing_retro_audio_rf_v2026_06_04_t0026_multi_window_context` from 4,598 rows across 18 sessions and added the 06-04 reviewed data without changing app artifacts. It corrects 26/30 baseline 06-04 target rows that were effectively `non_target` in the final in-sample focus check and 6/6 manual-missed rows, while older reference sessions remain within the safety gate. However, 06-04 leave-one-session-out racket recall is 0.812, so marker-level replay is required before promotion | Codex | T0027 should compare T0026 against the installed T0024 baseline on 05-28, 05-29, 06-03, and 06-04, sweep review thresholds if needed, and only recommend T0028 export/build/install if marker TP/wrong/FP/missed improves safely. `studs_live`, ordinary bounce, video models, and app UI stay out of scope | T0027 does not beat T0024 safely, or it shows threshold/candidate recovery matters more than the retrained RF |
| 2026-06-04 | Use a four-ticket sequence for the 2026-06-04 playing-retro review round | The first saved T0024 review, `audio_session_2026-06-04_001`, has 165 final audio markers, including 35 manual additions. T0025 confirmed most manual additions are near saved candidates: 21 were model `non_target`, 7 wrong racket/table class, 6 table predictions below threshold, 1 timing/dense case, and 0 material true candidate-generation gaps | Love + Codex | T0025 completed the pull/audit. T0026 retrains `spel_retro_audio` with 06-04 plus historical playing data; T0027 replays/tunes the candidate against the T0024 baseline; T0028 exports/builds/installs only if replay passes. T0029 candidate/peak recovery is deferred unless replay shows true candidate-generation gaps remain material. `studs_live`, ordinary bounce, and video models stay out of scope | T0027 shows retraining does not reduce misses, or a later reviewed session shows many manual additions with no usable candidate nearby |
| 2026-06-03 | Proceed to T0024 export/build/install with the T0022 `spel_retro_audio` model and a table confidence threshold | T0023 replay across `audio_session_2026-05-28_002`, `audio_session_2026-05-29_001`, `audio_session_2026-05-29_002`, and `audio_session_2026-06-03_005` showed a strong safe gain versus the current T0020 visible baseline: true positives 576 -> 687, wrong-class 22 -> 1, false positives 24 -> 8, and missed truths 118 -> 28. The selected replay uses the retrained T0022 model with racket threshold `0.0`, table threshold `0.5`, and the existing 80 ms same-label dedupe | Codex | T0024 should export only `playing_retro_audio_model.json` from `playing_retro_audio_rf_v2026_06_03_t0022_multi_window_context`, apply the selected playing-retro table threshold in the Review path, validate app/Python parity, then build/install on Motorola. `studs_live`, `audio_model.json`, `audio_contact_model.json`, and video models remain out of scope | Export parity fails, TypeScript/app replay disagrees with T0023, or Motorola review shows the selected threshold hides useful table bounces or still creates confusing extras |
| 2026-06-03 | Prioritize `spel_retro_audio` retraining before candidate-generator rewrites for the 2026-06-03 miss pattern | T0021 showed that 13 of 20 manual additions had a nearby candidate that the model classified as `non_target`, while only 2 were true candidate-generation gaps. Manual additions were mostly high-energy and dense-sequence events, so a model retrain from the new positives is the highest-value next step | Codex | T0022 should train candidate(s) from historical playing rows plus `audio_session_2026-06-03_005`, adding manual additions as positive target rows and keeping unmatched candidates as non-target rows. Candidate/recovery settings still matter in T0023, but the first retrain should not replace `studs_live` or ordinary-bounce artifacts | T0022 retrain fails to improve missed target recall, or T0023 shows recovery/candidate gates explain more misses than the T0021 analysis suggests |
| 2026-06-03 | Use normal ticket numbers for the retrain/evaluation sequence before the next new-video loop | T0020 only improved duplicate post-processing. Love confirmed the reviewed `audio_session_2026-06-03_005` clip should now be analyzed and used to improve `spel_retro_audio`, especially the 20 manual additions and missed table/racket hits, before asking him to review another new video. Love also wants ticket numbering to stay linear without letter suffixes | Love + Codex | T0021 audits why the current model missed/corrected markers, T0022 retrains a candidate with the 2026-06-03 clip and historical playing data, T0023 tunes/replays model plus settings against old and new sessions, and T0024 exports/builds/installs only if gates pass. The next new-video release loop is deferred to T0025. `studs_live` and ordinary up/down bounce remain out of scope | The retrain candidate does not beat T0020 safely, or Love chooses speed over the intermediate retrain cycle |
| 2026-06-03 | Use 80 ms same-label duplicate suppression for primary `spel_retro_audio` review markers | The first T0020 per-video audit on `audio_session_2026-06-03_005` found close same-label duplicate candidate pairs while many real racket/table pairs were also close together. An 80 ms same-label-only rule removes 7 duplicate target suggestions, keeps true positives unchanged, and preserves tight racket+table pairs | Codex | Primary `Ljud + video ML` playing review now dedupes generated `spel_retro_audio` markers only when the adjacent marker has the same final audio kind within 80 ms. It does not change `playing_retro_audio_model.json`, `audio_model.json`, `audio_contact_model.json`, or `studs_live` | A future reviewed video shows real same-label events under 80 ms that are hidden, or duplicate same-label markers remain a major review burden |
| 2026-06-03 | Use a per-video `spel_retro_audio` release loop before collecting more reviewed playing videos | Love reviewed the T0019 APK and found the new retro path promising, but still had to add some table/racket markers and saw close duplicate racket markers. Reviewing several new videos on a stale detector would create repeated correction work and weaker active-learning signal | Love + Codex | Future playing-retro work should process one reviewed video at a time: pull/audit the reviewed session, improve model/settings/UI, replay against the current baseline, build/install a new APK when better, and only then have Love review the next video with the new APK | The loop is too slow for data collection, or a backend/offline batch reviewer replaces Motorola APK iteration |
| 2026-06-03 | Use the current conservative T0014 recovery gates for the T0016 APK candidate | T0015 swept 1,764 class/gap threshold combinations against the Tomas/Stiga reviewed replay set. The current gate recovered the same maximum safe set as the best alternatives: 6 correct missed table bounces, 0 wrong-class near missed truth, 0 duplicate-near-baseline truth, and 0 visible false positives. No tested safe gate recovered extra racket hits, so lowering racket visibility now would add review risk without measured gain | Codex | App constants stay unchanged: racket recovery requires confidence >=0.80 and nearest saved gap >=120 ms; table recovery requires confidence >=0.54 and nearest saved gap >=60 ms. T0016 may build/install the candidate APK for Motorola review, with `Studsdetektor` unchanged | Device review shows too many missed dense racket hits, too many recovery suggestions, or a new reviewed playing session changes the replay gate |
| 2026-06-03 | Keep T0014 recovery conservative and defer aggressive racket recovery to T0015 | The first recovery replay recovered 6 additional table bounces with 0 visible false positives, but lower racket thresholds created duplicate racket suggestions near already matched truths. Love needs better dense recall, but not at the cost of confusing review with duplicate racket hits | Codex | T0014 uses a 28 ms recovery peak gap, table recovery threshold 0.54 with nearest saved gap >=60 ms, and racket recovery threshold 0.80 with nearest saved gap >=120 ms. T0015 should tune these gates, especially racket recovery, before T0016 APK build | T0015 replay shows safe racket recovery can recover missed racket hits without duplicate/false-positive cost, or Love chooses a more aggressive review workload |
| 2026-06-03 | Promote `spel_retro_audio` through T0013-T0016 before the next main APK test | Love confirmed the desired end state is normal editable Review using playing-retro audio, not a separate manual `Kor retro` comparison button. Building after only T0013 would still miss dense events that never became saved candidates, so the safer path is primary review first, dense peak recovery second, replay/threshold tuning third, and APK fourth | Love + Codex | T0013 makes playing-retro the primary classifier on existing saved candidates. T0014 adds dense missed-peak recovery. T0015 runs replay/tuning gates. T0016 builds/installs the Motorola APK while keeping `Studsdetektor` unchanged | Love explicitly requests an intermediate APK after T0013, or T0014/T0015 show the recovery layer is too risky and should be deferred |
| 2026-06-02 | Make the first visible `spel_retro_audio` Review path reclassify saved candidates only and not mutate marker truth | T0010 proved saved-candidate reclassification is strong, while generating additional peaks is a separate higher-risk detector problem. A manual Review panel lets Love inspect the retro model without silently changing training labels or live behavior | Codex | T0011 adds a `Spel-retro audio` panel and separate waveform pins. The button reclassifies current `model_candidates` with `playing_retro_audio_model.json`, but it does not create/save markers, does not affect `studs_live`, and does not replace `audio_model.json`. APK install waits for Love's explicit approval | Love needs the APK to auto-surface missed markers, or candidate-generation replay proves a safe way to recover the 15 missed Tomas/Stiga markers without too many false positives |
| 2026-06-02 | Do not build/install the `spel_retro_audio` APK until a visible Review path also handles candidate generation or surfacing | T0010 app-export replay on saved candidates is strong, but it only reclassifies candidates that already exist; the target Tomas/Stiga sessions still have 15 reviewed missed markers with no saved candidate to classify | Codex | Keep T0010 as a local replay/control step. Next ticket should wire a separate Review UI/replay path that can either surface retro classifications or generate additional candidate peaks before a release APK is useful | Love approves a Review-only APK test of reclassification alone, or T0011 adds candidate surfacing/generation and passes replay/TypeScript checks |
| 2026-06-02 | Keep `spel_retro_audio` as a separate Review-only app model export, not a replacement for `audio_model.json` | T0009 exported the validated T0007/T0008 model into `playing_retro_audio_model.json` with tight/normal/wide/context feature metadata and added an opt-in app runtime helper; this gives Review retro a testable path without changing live bounce behavior | Codex | Collector now has a separate `spel_retro_audio_review_only` model artifact and `playingRetroAudio.ts` helper. Normal Review candidates, `studs_live`, `audio_model.json`, and `audio_contact_model.json` remain untouched by this ticket. Next work should wire a controlled replay/UI path before any APK install | App-side replay disagrees with Python T0008 rows, Review integration is too slow, or Love approves replacing/adding visible Review behavior |
| 2026-06-02 | Allow the T0007 `spel_retro_audio` multi-window/context candidate to proceed toward a separate Review retro integration path | T0008 leave-one-session-out validation across `audio_session_2026-05-28_002`, `audio_session_2026-05-29_001`, and `audio_session_2026-05-29_002` shows the selected T0007 variant matches or improves T0006 racket recall on every requested holdout, with table recall unchanged and non-target equal or better | Codex | Candidate `playing_retro_audio_rf_v2026_06_02_multi_window_context` remains local, but the next ticket can wire a separate `spel_retro_audio` retro path in Review. This still must not replace `studs_live` or Collector `audio_model.json` without an explicit export/build ticket | App integration produces different features than the Python script, app replay disagrees with local predictions, or a new dense playing holdout regresses |
| 2026-06-02 | Keep the T0007 `spel_retro_audio` multi-window/context candidate local and validate cross-session before app integration | The selected variant materially improves Tomas backhand holdout performance, but it has only been selected on `audio_session_2026-05-29_002` and ordinary-bounce fallback metrics are advisory because older ordinary rows lack exact multi-window timestamps | Codex | Local candidate `playing_retro_audio_rf_v2026_06_02_multi_window_context` is saved under ignored model artifacts. No Collector model JSON, APK, or `studs_live` behavior changes. Next work is leave-one-session-out or cross-session validation for dense playing sessions before wiring a separate Review retro path | Cross-session validation fails, or it passes agreed dense-playing gates and a separate retro app path is ready to consume the new feature shape |
| 2026-06-02 | Keep the T0006 `spel_retro_audio` safe racket-weighted candidate local and do not integrate it into Review yet | Focused one-window weighting improves Tomas backhand holdout racket recall only modestly, from 0.604 to 0.623, while a more aggressive racket variant improves recall more but drops non-target recall to 0.500 | Codex | Local candidate `playing_retro_audio_rf_v2026_06_02_safe_racket_weighted` is saved under ignored model artifacts, but no Collector model JSON, APK, or `studs_live` behavior changes. Next useful work is true multi-window and non-leaky candidate-context features, not app export | A multi-window/context candidate materially improves racket recall on `audio_session_2026-05-29_002` without losing table/non-target protection and without ordinary-bounce regression |
| 2026-06-02 | Train the first `spel_retro_audio` candidate from saved app candidates plus manually missed markers, not replay-expanded peaks | The app's saved candidates are the candidate surface Review actually created and misunderstood, while replay variants are useful diagnostics but would multiply the same WAV by timing config and distort training balance | Codex | T0005 candidate `playing_retro_audio_rf_v2026_06_02_app_candidates_100_200` stays local, trains on all matchable saved app candidates, labels unmatched app candidates as `non_target`, adds reviewed missed racket/table markers at their timestamps, and reports ordinary bounce separately without changing `studs_live` | Error analysis shows replay candidate timing is needed as model input, or a later retro pipeline no longer uses app-saved candidate peaks as the candidate surface |
| 2026-06-02 | Retire IMU/AirHive from active product scope and focus the project on audio + video | Love confirmed the external sensor path is no longer interesting for this project; the current product direction is the three discussed model areas: `studs_live`, `spel_retro_audio`, and `video_stroke_retro` | Love + Codex | Project docs now treat IMU/AirHive specs, skills, scripts, and app surfaces as legacy/historical unless a future ticket explicitly reopens sensor work. New implementation tickets should not plan IMU collection, IMU model training, or audio+IMU fusion | Love explicitly decides to reintroduce external sensors or a client requirement makes sensor fusion necessary |
| 2026-06-02 | Use one scoped Codex ticket at a time for future implementation work | The audio/video/model work has become too complex to handle as broad chat-driven tasks; Love wants smaller tickets with explicit scope, allowed areas, non-goals, validation, and handoff so agents do not train, export, or edit unrelated systems accidentally | Love + Codex | `CODEX_TASK.md` is the single active ticket, `REPO_CURRENT_STATE.md` tracks repo status/completed tickets/next ticket, `FOLLOWUPS.md` stores out-of-scope work, and `AGENTS.md` requires reading these files before implementation | Ticket overhead blocks practical progress or a dedicated external issue tracker replaces local ticket files |
| 2026-06-01 | Keep the 06-01 Tomas backhand audio retrain candidate-only and hide non-review-relevant audio pins | Love confirmed the new reviewed file is trainable dense Tomas/Stiga backhand data but must not affect ordinary up/down bounce. The retrain improves the new dense backhand replay strongly in fast config, but adds false positives on other Tomas/Stiga sessions and +1 FP on ordinary 05-13 normal replay; hidden blue analysis candidates also made Review look like it contained deleted, untouchable table markers | Love + Codex | `audio_4class_2026-06-01_tomas_backhand_C_hybrid_100_200_60_140_gap300` stays local and is not exported to `apps/collector/src/models/audio_model.json`. Normal Review now shows only review-relevant audio candidate pins; non-review-relevant candidates remain saved as analysis data | A future candidate improves Tomas/Stiga dense recall without extra false positives on 05-28/05-29 Tomas and without any ordinary up/down bounce regression, or the app gets a separate playing-dense detector/config |
| 2026-05-29 | Keep the 05-29 Tomas hard audio retrain candidate-only | The new reviewed file is trainable and important for racket+table Stiga/Tomas hard sounds, but replay shows the retrain adds only one true positive on the new session while adding five false positives there and one extra false positive on ordinary quiet racket bounce | Codex | `audio_4class_2026-05-29_tomas_stiga_hard_C_hybrid_100_200_60_140_gap300` is kept as a local model-dir candidate and is not exported to `apps/collector/src/models/audio_model.json`, not built into an APK, and not installed. The current installed 05-28 Tomas/Stiga audio model remains the app artifact. | A future candidate improves Stiga/Tomas racket+table recall without increasing false positives on the new session and without regressing ordinary racket bounce |
| 2026-05-29 | Make `Ljud + video ML` motion review a whole-video FH/BH pass | Anchoring pose analysis only to confirmed audio hits can miss the filmed player's actual swing or be pulled toward opponent/contact timing; Love wants Review 2 to behave like the dedicated video-only FH/BH review after the audio pass | Love + Codex | `Ljud + video ML` stays staged: Review 1 is normal audio truth, while pose scans the full MP4 at 15 fps in the background and Review 2 shows separate FH/BH/Oklart motion markers on the waveform-like timeline. Audio rows and motion rows remain separate training layers. | Whole-video pose creates too many false positives, becomes too slow on long clips, or a future target-player/ball gate makes audio anchoring reliable again |
| 2026-05-28 | Require explicit audio-session intake metadata before training | Stiga/Tomas sessions can sound different from ordinary vertical racket bounce because of room acoustics, impact force, and play style; silently adding such data can improve one bucket while regressing another | Love + Codex | `pingis-audio-classification` now requires asking Love whether each new audio file/session is trainable, diagnostic, or holdout; which evaluation bucket it belongs to; impact style/force; environment/background; phone/device/player metadata; and whether it may influence ordinary bounce or only a domain-specific bucket. Reports must stay bucketed before promotion. | The app stores enough structured metadata at capture time that the agent no longer needs to ask, or a backend dataset registry owns intake state |
| 2026-05-28 | Build the Tomas Stiga-office audio retrain only for gated Motorola validation and suppress same-label dense review duplicates | Replay improved after adding Tomas 28/5 data, but training on the same Tomas take is not proof of live generalization; the review JSON also showed close racket/racket auto-candidate pairs that should not become visible markers | Codex | `collector_audio_4class_v2026_05_28_tomas_stiga_C_hybrid_window_candidate` is bundled in a release APK for pending Motorola validation, but the APK is not installed until Love approves. Dense audio review hides same-label duplicate suggestions inside 180 ms while keeping close table-to-racket and racket-to-table event pairs visible. | Motorola live validation shows the candidate beats 05-27 Stiga on Tomas plus older dense/noise/table cases without new double counts |
| 2026-05-28 | Reintroduce a dedicated video-only FH/BH collection path | `Ljud + video ML` is still the right flow when audio truth matters, but testing camera-only stroke detection needs a simpler import-review-save loop without audio anchors or opponent-hit timing | Love + Codex | Setup exposes `Video FH/BH` under DATA. It imports one MP4, extracts embedded audio only for waveform display, extracts ML Kit pose at 15 fps, proposes motion candidates with the current video-stroke model, and saves `video_stroke_session_*.json` under `/Download/pingis_video_stroke_sessions` for offline retraining. Review mirrors the normal audio review surface: audible source audio, WAV timeline, 16x zoom, playhead/timeline long-press marker creation, previous/next marker nav, same marker utility controls, and selected stroke playback. | The video-only detector produces too many/too few candidates compared with the staged audio+video review, or a backend review tool replaces local video review |
| 2026-05-27 | Hide auto `unknown/no-target` video motion candidates by default | Pose runs around every confirmed racket-contact audio marker, including opponent contacts, so visible `unknown` markers made Review slower without being FH/BH suggestions | Love + Codex | `Ljud + video ML` keeps `unknown` pose outputs as analysis candidates, but only concrete FH/BH candidates become visible motion markers automatically; manual `Oklart` markers can still be added for training truth. Anchored audio-to-motion review uses pose-window quality plus model confidence, not the harder whole-video scan motion gate. | We need more negative video truth directly in app review or hidden unknown candidates start hiding useful correction work |
| 2026-05-26 | Keep reviewed dense table bounces as training truth even when close to racket contacts | The bad 2026-05-26 audio candidate happened because close reviewed table markers were skipped as unsafe negatives, leaving the model with dense racket rows but almost no dense table rows | Codex | `preprocess_audio.py` keeps `table_bounce` markers while still skipping unsafe noise/floor/other negatives near racket; candidate `collector_audio_4class_v2026_05_26_dense_001_C_hybrid_window_candidate` is exported for testing but not installed by default | Full-chain replay or Motorola testing shows close table markers contaminate racket classification more than they help dense play |
| 2026-05-26 | Revert the 2026-05-26 audio candidate and quarantine diagnostic session 002 | The CJ backhand audio candidate counted almost everything as racket in `audio_session_2026-05-26_002`, and Love saved the broken review only to show the failure | Love + Codex | Collector returns to the 2026-05-25 dense CJ 007 audio baseline; `audio_session_2026-05-26_002` is excluded from audio and video-stroke preprocessing | A future candidate beats the 05-25 baseline in replay plus Motorola live validation without over-counting |
| 2026-05-26 | Treat reviewed `unknown` motion rows as video-stroke negatives | CJ backhand video showed that the old two-class FH/BH model confidently labeled opponent/non-visible hits as backhand when CJ held a backhand pose | Love + Codex | Video-stroke preprocessing can include reviewed/ignored `unknown` motion markers as a third training class; app inference maps that raw class to `uncertain` instead of FH/BH | The three-class model hurts true FH/BH recall too much or a better visible-player motion gate replaces the unknown class |
| 2026-05-26 | Treat live bounce as its own evaluated chain before changing the baseline | Review can recover events from whole WAVs, but live can miss sounds before the model ever sees them because native onset, spectral gate, retrigger, group, or merge blocked the candidate | Love + Codex | Collector keeps `Normal / 4-klass`, confidence `0.65`, `-100/+200 ms`, `Retrigger 220`, `Group 80`, `Merge 220` as baseline while adding native debug payloads, local live-debug JSON, and a replay script for WAV+review-marker testing | Full-chain replay plus Motorola live testing proves a new config/model improves recall without counting speech/music |
| 2026-05-25 | Remove the separate `Videoinsamling FH/BH` flow | The standalone video-only FH/BH collection path was not usable enough and duplicated the newer `Ljud + video ML` review infrastructure | Love + Codex | The main app no longer exposes the separate video collection card; video-only review should enter through `Ljud + video ML` and use `Hoppa över ljud` to run motion review over the whole video | A dedicated video-only review product becomes useful again with a stable non-audio workflow |
| 2026-05-22 | Track camera side for video stroke data separately from handedness | Player-left vs player-right camera placement changes the 2D pose feature distribution even when handedness is the same, and the CJ player-right forehand import exposed this as a real generalization gap | Love + Codex | Video stroke datasets carry `camera_side` metadata (`player_left` or `player_right`) for balancing, evaluation, and future normalization, but it is excluded from RandomForest features for now | A mirror/normalization strategy proves camera side can be made invariant, or the product supports arbitrary camera placement |
| 2026-05-19 | Make `Ljud + video ML` a staged audio-then-motion review | Reviewing audio hits and pose hits side by side became too cluttered; Love wants the existing audio workflow first and FH/BH review only after racket hits are confirmed | Love + Codex | Audio/video pose now saves one full WAV+MP4 event, can import MP4 with embedded audio, and runs pose around confirmed racket hits in Review 2 while keeping audio and motion training rows separate | Staged review is too slow for long sessions, or pose-only missed swings become important enough to scan whole video again |
| 2026-05-19 | Show `Ljud + video ML` as two synchronized review timelines | A list of pose suggestions made review hard to reason about; Love needs audio hits and motion hits visible on parallel time axes with inline editing | Love + Codex | Audio markers and motion markers are separate rows, low-confidence audio suggestions stay as debug candidates, and the combined marker panel is hidden in this flow | Parallel timelines make review slower than the previous combined marker list or a later review tool handles multi-layer events better |
| 2026-05-19 | Make `Ljud + video ML` review manual-sync and two-layer labeled | Sync must survive noisy recordings where auto picks the wrong clap, and audio truth should not be conflated with pose truth | Love + Codex | Review stores manual/auto sync anchors, separates audio labels from motion labels, replaces the IMU preview with a pose-candidate panel, and gates pose suggestions harder before FH/BH classification | Manual sync remains too slow, pose-only review creates too many false positives, or a backend review tool takes over labeling |
| 2026-05-19 | Add unified `Ljud + video ML` collection on top of audio review | The existing audio collection/review flow already owns timing, waveform, video sync, and human ground truth; pose should be an added candidate layer rather than a separate product review surface | Love + Codex | Collector gets a new long audio+video capture mode that splits to 30 s review takes, stores audio candidates and pose candidates separately, and lets review markers link both layers | The combined review becomes too cluttered, pose processing is too slow for practical review, or a later backend/session browser replaces local review queues |
| 2026-05-18 | Split one long Video stroke source into 30 s review takes | Sequential 30 s recordings caused review/playback fragility and made long capture awkward; a single source MP4 preserves capture continuity, while native Android splitting keeps the existing review/training take format | Love + Codex | `Videoinsamling FH/BH` has an unlimited-recording toggle that saves one source `.mp4` and then creates roughly 30 s `.mp4` review takes in the same session | Native split output is slow, unplayable on device, or exact 30 s boundaries become more important than keyframe-safe playback |
| 2026-05-18 | Allow Video stroke resume and import | A long collected match can be lost from the in-memory review state if the user exits the screen, but the saved source/segments are still valuable training data | Love + Codex | `Videoinsamling FH/BH` can reopen the latest saved video-stroke session and can import a selected `video/*` file into a new session before splitting it into review takes | A persistent session browser or cloud-backed collection queue replaces local JSON/session recovery |
| 2026-05-18 | Put video stroke collection under `DATA` and follow Review ergonomics | Love treats this as video data collection, not a test mode; the review surface should inherit the useful controls from existing audio/video review instead of diverging | Love | Setup shows `Videoinsamling FH/BH` in the Data section, and video-stroke review now has adjustable playback speed like the older review flow | Video stroke becomes only a hidden debug experiment or is folded into a unified collection/review module |
| 2026-05-18 | Treat video stroke auto detections as review suggestions only | The app can propose likely FH/BH events, but those predictions must not become training truth before Love confirms or corrects them | Love + Codex | `Video stroke test` can auto-add model suggestions; preprocessing trains only on `source=manual` and `review_status=confirmed` markers | A robust active-learning workflow or backend review state replaces local JSON review |
| 2026-05-18 | Default Collector setup to left-handed for Love's current video-stroke branch | Session 003 was accidentally saved as right-handed even though Love is left-handed, causing the app/model to inspect the wrong arm | Love | Setup starts with `LEFT HAND` selected, and session 003 metadata was corrected locally and pushed back to the Motorola JSON | Multi-player collection requires neutral defaults or named player profiles |
| 2026-05-18 | Add a separate Android video stroke test for FH/BH classification | Forehand/backhand from a front/snett-framifrån camera is feasible and lower risk than ball tracking, but should not disturb the active audio detector pipeline | Love + Codex | Collector gets a separate `Video stroke test` flow, separate storage under `pingis_video_stroke_sessions`, ML Kit pose extraction, and a local RandomForest video-stroke training/export path | The test cannot collect usable pose markers, app build/runtime becomes too heavy, or audio+IMU fusion makes the camera path unnecessary |
| 2026-05-13 | Set Collector bounce live baseline to `collector_bounce_live_v2026_05_13_normal_4class_220_80_220` | Motorola testing showed `Normal / 4-klass` with `Retrigger 220`, `Group 80`, `Merge 220` counts quiet racket bounce reliably (`40/40`) while `Group 0` can double-count (`41/40`) | Love + Codex | This named bundle becomes the single source of truth for Collector live bounce counting and the artifact/config id to copy into `stiga-app-v2`; confidence remains adjustable in live/review | A new model/window/config beats this baseline in full-chain replay and Motorola live validation |
| 2026-05-13 | Add dense racket/table play as an explicit collection scenario | Sessions 008/009 showed that realistic table/racket alternation is learnable but underrepresented; generic imported/audio labels hide the gap | Love + Codex | `Ljudinsamling` gets `Spel: racket + bord`; `Audio plus IMU` gets `Playing: racket + bord`; local sessions 008/009 are classified as dense play for future analysis | Dense-play performance no longer needs separate tracking or a dedicated playing model replaces the shared 4-class model |
| 2026-05-13 | Treat human-reviewed markers as the single source of truth and legacy as weak/bootstrap only | The latest source/window/model tests show that legacy data can improve coverage, but it must not silently define ground truth; live export decisions must be based on reviewed markers plus full-chain replay | Love + Codex | Training reports must separate `human_reviewed`, `legacy_auto`, and augmented rows; Collector exports should not be chosen from static train metrics alone | Reviewed-only data has enough balanced table/floor/noise coverage to outperform weak legacy on grouped and live-chain replay |
| 2026-05-13 | Use full live-chain replay before the next Collector audio export | The Stiga app appears better mostly because its candidate gate/onset pipeline differs, not because its model JSON is inherently stronger | Codex | Next Collector candidate should align onset/gate/merge behavior and then be replayed on reviewed WAV+JSON before building an APK | Motorola live validation and replay agree for quiet, noisy, speech, table, floor, and dense table/racket sessions |
| 2026-05-13 | Roll back Studsdetektor from tight live inference to the 2026-05-12 stable live pair | Motorola testing showed the tight-overlap export regressed live counting: missed quiet racket bounces and counted normal speech when sensitivity increased | Love + Codex | Collector APK is rebuilt with the 2026-05-12 audio model pair and the previous 1 s live/review model window; tight candidates remain local experiments only | A full-take simulator proves a new window/model combination beats the rollback on reviewed WAV+JSON and then passes Motorola live validation |
| 2026-05-13 | Match collector inference windows to exported tight audio models | The 2026-05-13 export was trained on short reviewed marker windows; feeding the app's old 1 s `-300/+700 ms` clips would create a train/runtime mismatch | Codex | Review candidate generation and live native audio streaming now classify `-60/+140 ms` model clips, while marker preview playback can remain longer for human listening | Device validation shows tight app inference misses too many real racket contacts or needs more context |
| 2026-05-12 | Skip unfinished review-required takes in audio preprocessing | A session can contain a just-recorded take before Love has reviewed it; those takes must not fall back to legacy auto-training and become accidental ground truth | Codex | `preprocess_audio.py` now skips events with `review.required` when `completed_at` is missing or marker count is zero | A dedicated draft-take state replaces the current JSON shape |
| 2026-05-12 | Export the retrained 2026-05-12 audio model pair | Today's reviewed noise and noisy-floor data improved the 4-class model and produced a stronger binary `all_legacy` contact candidate than the active app artifact | Codex | `apps/collector/src/models/audio_model.json` and `apps/collector/src/models/audio_contact_model.json` are updated; APK still needs a build/install before device testing | Motorola live validation shows worse counting or worse surface/noise behavior |
| 2026-05-12 | Do not export the first high-music-only retrain candidate | Adding `audio_session_2026-05-12_004` gave useful high-music coverage, but the retrained candidate lowered overall macro-F1 compared with the already exported 05-12 model pair | Codex | Candidate artifacts are kept under `data/audio/models`, while the app model JSON files remain on the stronger 05-12 001/002 export | More high-music racket takes improve both scenario and overall metrics |
| 2026-05-11 | Treat `audio_session_2026-05-11_002` as trainable reviewed music data | Love confirmed both takes in the session are valid training data after review | Love | The session may be copied into `data/audio/raw` and included by preprocessing/training; it contains two `racket_music_low` takes | Review finds incorrect labels in the session |
| 2026-05-11 | Export the retrained 4-class audio model, but keep the binary contact model unchanged | The 4-class candidate improved on today's reviewed sessions and slightly improved grouped macro-F1, while the binary contact candidate still showed weak hard-negative robustness on grouped evaluation | Codex | `apps/collector/src/models/audio_model.json` is updated from `audio_4class_2026-05-11_today`; `audio_contact_model.json` remains the previous active model | Motorola live/review validation shows the new 4-class model hurts veto/debug behavior |
| 2026-05-11 | Exclude `audio_session_2026-05-11_001` from training | Love explicitly marked this session as diagnostic-only after it was used to compare review configs | Love | The session can be inspected for config behavior, but must not be copied into training raw data or used in preprocess/train runs | Love explicitly reclassifies the session as trainable after review |
| 2026-05-11 | Add explicit detection config, model-candidate, and review-truth layers | The app must capture what the algorithm proposed, which settings produced it, and what Love corrected without letting auto detections become training truth | Love + Codex | New takes store a normal/hybrid detection config snapshot, model candidates are saved separately from reviewed markers, and preprocessing reports misses/false positives while training only on human-reviewed markers | The collector needs on-device adaptation or a backend model registry owns experiment metadata |
| 2026-05-06 | Use natural racket-arm motion as bounce-IMU negative data | The negative class should be varied real racket-arm movement without ball contact, not a fake bounce rhythm that creates ambiguous ground truth | Love + Codex | Guided Audio plus IMU hides `Mixed studs`, adds `Racketrörelse utan studs`, allows markerless save, and samples whole-take IMU windows as `not_bounce_contact` | No-bounce motion fails to improve bounce-IMU or a better negative sampling strategy is validated |
| 2026-05-06 | Treat noisy and fast audio-only collection as first-class data | Quiet-only racket sounds do not cover real rooms, speech, music, other bounces, or normal fast bounce spacing | Love + Codex | `Ljudinsamling` gets focused noisy/fast presets, reviewed markers use short event-centered preprocessing, and reports break down spacing/background | Scenario-level metrics show noisy/fast presets do not add useful coverage or the collection UI becomes too heavy |
| 2026-05-05 | Limit Playing review to FH hit, BH hit, and table bounce | Long match reviews need a compact label set focused on the events Love actually marks, while audio confidence can still filter auto-candidates | Love + Codex | Playing markers use `forehand_hit`, `backhand_hit`, or `table_bounce`; low-confidence auto-candidates can be stored as filtered and excluded from training | Playing review needs floor/noise labels again or a trained IMU stroke model can pre-label FH/BH safely |
| 2026-05-05 | Move `Playing` under `Audio plus IMU` | Free longer review-first capture is an audio+movement scenario, not a separate top-level data mode | Love + Codex | Startsida Data shows `Ljudinsamling` and `Audio plus IMU`; audio+IMU events carry high-level `scenario` and optional `bounce_context` metadata | Playing needs a fundamentally different product entrypoint or audio/video-only free recording becomes user-facing again |
| 2026-05-05 | Make Audio plus IMU pose calibration optional after table baseline | Raw IMU should still be collectible when FH/BH poses are not useful or would slow down collection | Love + Codex | Calibration stores `captured` vs `partial`; FH/BH poses are helper metadata, not playing stroke labels | A trained side classifier requires pose calibration as a hard prerequisite |
| 2026-05-05 | Limit guided Audio plus IMU hard negatives to audio-only collection | Table/floor/noise do not need IMU in v1, while racket-bounce side coverage does | Love + Codex | Audio plus IMU stores racket-bounce FH-side, BH-side, mixed, and playing; hard negatives stay in `Ljudinsamling` | IMU becomes useful for non-racket audio classes |
| 2026-05-05 | Separate audio-only, audio+IMU, and free-recording data modes | Audio-only cannot truthfully label FH/BH, while synced IMU/free recording need richer reviewed event labels | Love + Codex | Ljud-insamling collects generic sound classes; Studs audio + IMU keeps FH/BH prompts; Fri inspelning records long review-first takes with structured marker labels | A later product flow proves one unified collection mode is simpler without hurting label quality |
| 2026-05-05 | Hide legacy `Datainsamling` from the main Data section | The card leads to the older IMU-only `DataCollectionScreen`, while current user-facing collection is `Ljud-insamling` and `Studs audio + IMU` | Love + Codex | Legacy route/code remains available internally, but the startsida only shows relevant collection flows | A debug/developer menu is added or legacy IMU-only collection becomes user-facing again |
| 2026-05-04 | Use a visible and audible sync event for review video calibration | `Date.now()` between separate audio/video recorders is useful as a default but too coarse for easy manual review alignment | Love + Codex | Collection prompts a clap/tap at take start; Review detects the early audio sync spike and computes `video_sync_offset_ms` from the selected video frame | Video recording can safely include its own audio track, or measured drift shows a constant offset is not enough |
| 2026-05-04 | Use an immersive, current-session recording flow | Collection should feel like a camera flow and should not expose stale pending samples during new data collection | Love + Codex | Android hides system bars; collection uses countdown, fixed controls, and current-session pending review only | Device testing shows immersive mode or hidden old queue makes collection harder |
| 2026-05-04 | Target 150 Hz raw IMU collection when stable | 50 Hz was a baseline assumption, not a proven limit; higher raw resolution can be downsampled later | Love + Codex | Takes record `target_hz`, measured sample rate, interval stats, quality flag, and AirHive `sensor_ts` without blindly writing BLE config | BLE/device stability is worse at higher rates or AirHive confirms a lower hardware limit |
| 2026-05-04 | Make human-reviewed markers the primary contact-training truth | Legacy data can bootstrap and compare variants, but the algorithm must not create its own final labels | Love + Codex | Default contact preprocessing uses reviewed markers; legacy variants are opt-in via `human_reviewed`, `legacy_hybrid`, and `bootstrap` | Reviewed-only metrics fail to catch up after enough reviewed coverage |
| 2026-05-04 | Treat FH/BH racket-bounce as metadata, not audio classes | The contact sound should stay `racket_contact`; FH/BH coverage and side belong in prompts and IMU/debug metadata | Love + Codex | Collection presets split FH/BH/mixed for coverage, but audio labels remain binary/multiclass surface labels | Evidence shows FH/BH produces reliably different audio that improves count quality |
| 2026-05-04 | Separate bounce IMU from stroke IMU | Repeated racket bounces and real in-play forehand/backhand strokes are different ML problems | Love + Codex | Bounce IMU windows use reviewed audio markers; stroke IMU gets its own x-axis feature engineering | A later fusion model needs a shared representation after both datasets are stable |
| 2026-05-04 | Add live contact grouping after native onset and JS merge | Native retrigger suppresses raw onsets, while JS grouping suppresses duplicate qualified contacts from one physical contact | Love + Codex | Debug rows expose `group_id`, `best_candidate`, and `ignored_duplicate`; counted contacts are capped per group | Device validation shows grouping drops true fast contacts too aggressively |
| 2026-04-22 | Keep audio as the primary contact truth | Reviewed audio data is the strongest current signal for racket contact | Love + Codex | Audio model remains live count engine | Audio fails stable device validation after targeted data and live-merge fixes |
| 2026-04-22 | Do not replace audio with IMU | Bounce motion and real stroke motion are separate problems | Love + Codex | IMU work stays additive/removable | IMU evidence clearly outperforms audio in controlled tests |
| 2026-04-22 | Use one reviewed audio timeline as label truth | Keeps labels consistent across audio-only and synced audio+IMU collection | Love + Codex | Review markers supervise current audio and future synchronized data | Review UI becomes unreliable or a new sensor requires separate labels |
| 2026-04-22 | Use `0.5-1.0 s` spacing for base bounce collection | Cleaner clips are easier to review and train initially | Love + Codex | Fast double-bounce scenarios are deferred | Base detector becomes stable enough for fast-contact data |
| 2026-04-22 | Include legacy 4-class data in binary contact training via `all_legacy` | It improved binary contact behavior compared with stricter current-only datasets | Love + Codex | `all_legacy` is current best binary strategy | A reviewed-only dataset exceeds it across scenario-level metrics |
| 2026-04-23 | Keep training the 4-class model alongside the binary model | Table/floor/noise separation is useful for veto/debug and future behavior | Love + Codex | Two audio models remain active in app artifacts | 4-class model stops adding veto/debug value |
| 2026-04-24 | Pause IMU work and prioritize audio stability | Duplicate counts, missed angled contacts, and floor false positives are audio blockers | Love + Codex | New work should target audio data, live merge, and evaluation first | Audio meets agreed live validation thresholds |
| 2026-04-24 | Treat review video as labeling support, not model input | Video helps humans review but would create a new ML problem before audio is stable | Love + Codex | Training pipeline continues to use WAV/features and review markers | Audio review cannot be made trustworthy without video-derived labels |
| 2026-04-30 | Adopt the WRLDS template workflow in this repo | Future AI agents need project context, decision history, skills, and validation in-repo | Love + Codex | `PROJECT_CONTEXT.md`, `DECISIONS.md`, template skills, references, and validation scripts become part of the project | Template workflow blocks practical project work |

## Active Constraints

| Constraint | Source | Impact | Revisit Trigger |
|---|---|---|---|
| Do not push directly to `main` | WRLDS workflow | Work must happen on a feature branch and merge through PR/review | Explicit repository policy change |
| Do not commit unless explicitly asked | WRLDS workflow | Agents can edit locally but need user intent before committing | Explicit user request to commit/push |
| Update `ITERATION_LOG.md` after model/data/build feedback | Project workflow | ML state must survive handoff between AI agents | A better model registry replaces it |
| Keep raw data out of git | Data size/privacy | `/data/` and large local exports stay local | A proper data storage/sync system is introduced |

## Deferred Decisions

| Decision | Why Deferred | Needed By | Owner |
|---|---|---|---|
| Whether to use AWS/backend sync | Current loop is local device-to-computer | Before multi-user or cloud data collection | Love + WRLDS |
| Whether to remove or hide legacy IMU app surfaces | Sensor work is retired, but code/UI may still contain legacy names and flows | Before a user-facing release or next Collector cleanup ticket | Love + Codex |
| Whether to change model family beyond RandomForest | Current blockers are data coverage and live event logic first | If targeted data + merge fixes do not solve live failures | Codex |

## Reversed Decisions

| Date | Reversed Decision | Replacement Decision | Rationale | Decided By |
|---|---|---|---|---|
| TBD | TBD | TBD | TBD | TBD |
