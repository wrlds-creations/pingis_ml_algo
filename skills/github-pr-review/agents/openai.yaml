interface:
  display_name: "GitHub PR Review"
  short_description: "Address PR feedback by theme"
  default_prompt: "Use $github-pr-review to group PR feedback, fix straightforward items, ask before larger changes, and summarize what remains."
